import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "prune", description: "Clean old package versions" },
  run() {
    console.log("pkgl prune: not implemented yet");
  },
});
