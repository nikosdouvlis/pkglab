import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "ls", description: "List linked consumer repos" },
  run() {
    console.log("pkgl repos ls: not implemented yet");
  },
});
