import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "activate", description: "Activate repo for auto-updates" },
  run() {
    console.log("pkgl repos activate: not implemented yet");
  },
});
