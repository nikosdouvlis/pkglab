import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "deactivate", description: "Deactivate repo" },
  run() {
    console.log("pkgl repos deactivate: not implemented yet");
  },
});
