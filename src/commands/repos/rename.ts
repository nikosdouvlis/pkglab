import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "rename", description: "Rename a repo alias" },
  run() {
    console.log("pkgl repos rename: not implemented yet");
  },
});
