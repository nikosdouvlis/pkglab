import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "ls", description: "List packages in Verdaccio" },
  run() {
    console.log("pkgl pkgs ls: not implemented yet");
  },
});
