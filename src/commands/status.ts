import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "status", description: "Show server info and status" },
  run() {
    console.log("pkgl status: not implemented yet");
  },
});
