---
"pkglab": minor
---

Auto-detect catalog entries when adding packages: pkglab add now checks if a package exists in a workspace catalog and automatically uses catalog mode, removing the need for the --catalog flag in most cases
