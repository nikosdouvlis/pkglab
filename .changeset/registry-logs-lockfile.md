---
"pkglab": minor
---

Redirect registry worker output to log file so `pkglab logs -f` shows pings and publish events. Fix pnpm lockfile patching for monorepo sub-package consumers by walking up to find pnpm-lock.yaml. Add lockfile patch status to pub spinner output.
